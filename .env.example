# Optional: GitHub personal access token for fetching contributors at build time.
# Without it you get 60 requests/hour (usually enough for small projects).
# With it: 5000 requests/hour. Create one at https://github.com/settings/tokens
# Only needs the default "public_repo" read access (no special scopes required).
GITHUB_TOKEN=
